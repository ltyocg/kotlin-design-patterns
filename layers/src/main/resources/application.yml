spring:
  main:
    web-application-type: none
  datasource:
    url: jdbc:h2:mem:databases-cake
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
